# IMPORTANT: This manifest depends on a fault-injection-aware backend image.
# The default prequal-backend:latest image does NOT implement FAULT_PROBE_MODE.
# This manifest defines the deployment shape, ingress wiring, and env var contract
# for when a fault-injection backend exists, or for any engineer who builds a custom
# image that implements FAULT_PROBE_MODE=malformed behavior (probe returns malformed body).
# See benchmark/evidence-asset-spec.md section 10 for details.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bench-fault-malformed
  namespace: prequal-benchmark
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bench-fault-malformed
  template:
    metadata:
      labels:
        app: bench-fault-malformed
    spec:
      containers:
      - name: backend
        image: prequal-backend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        - name: WORK_MULTIPLIER
          value: "1.0"
        - name: FAULT_PROBE_MODE
          value: "malformed"
---
apiVersion: v1
kind: Service
metadata:
  name: bench-fault-malformed
  namespace: prequal-benchmark
spec:
  selector:
    app: bench-fault-malformed
  ports:
  - name: http
    port: 8080
    targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bench-fault-malformed
  namespace: prequal-benchmark
  labels:
    ingress.class: prequal
  annotations:
    lb/algo: prequal
spec:
  rules:
  - host: fault-malformed.bench.local
    http:
      paths:
      - path: /work
        pathType: Prefix
        backend:
          service:
            name: bench-fault-malformed
            port:
              number: 8080
